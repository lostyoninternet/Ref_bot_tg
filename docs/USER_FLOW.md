# Блок-схема для пользователя: сообщения бота и этапы

Путь участника показан **блок-схемами** с примерами реальных сообщений бота и кнопок. **Обязательная подписка на канал:** без неё бот не даёт доступ к ссылке и разделам (только заявка на очный этап и проверка подписки).

---

## Единая блок-схема: все этапы и возможности

```mermaid
flowchart TB
    START([Пользователь открывает бота])

    subgraph STAGE1[Этап 1. Вход]
        START --> CMD{Команда или действие}
        CMD -->|/start| START_MSG[Бот: приветствие, описание программы. Доступ после очного этапа и подписки на канал]
        CMD -->|Любая другая кнопка| BLOCK[Бот: нужно пройти очный этап и подписаться на канал]
        START_MSG --> BT1[Кнопки: Заявка на очный этап, Проверить подписку]
        BLOCK --> BT1
        BT1 --> CHECK{Нажал Проверить подписку?}
        CHECK -->|Нет подписки| ALERT[Бот: Ты ещё не в закрытом канале. Подпишись после очного этапа]
        ALERT --> BT1
        CHECK -->|Есть подписка| CONFIRM[Бот: Подписка подтверждена]
    end

    CONFIRM --> HAS_EMAIL

    subgraph STAGE2[Этап 2. Регистрация]
        HAS_EMAIL{Есть email?}
        HAS_EMAIL -->|Нет| REQ_EMAIL[Бот: Введи email с регистрации на сайте]
        REQ_EMAIL --> INPUT_EMAIL[Пользователь вводит email]
        INPUT_EMAIL --> EMAIL_OK{Формат верный?}
        EMAIL_OK -->|Нет| BAD_EMAIL[Бот: Неверный формат email]
        BAD_EMAIL --> INPUT_EMAIL
        EMAIL_OK -->|Да| SAVED_EMAIL[Бот: Email сохранён. Укажи телефон]
        SAVED_EMAIL --> INPUT_PHONE[Кнопка Поделиться контактом или ввод номера]
        HAS_EMAIL -->|Да| HAS_PHONE{Есть телефон?}
        HAS_PHONE -->|Нет| SAVED_EMAIL
        INPUT_PHONE --> PHONE_OK{Номер верный?}
        PHONE_OK -->|Нет| BAD_PHONE[Бот: Введи корректный номер]
        BAD_PHONE --> INPUT_PHONE
        PHONE_OK -->|Да| DONE[Бот: 3 сообщения - приветствие, фото рюкзака, кнопка кабинета]
        HAS_PHONE -->|Да| MENU[Основное меню]
        DONE --> MENU
    end

    MENU --> CHOICE

    subgraph STAGE3[Этап 3. Кабинет и приглашение]
        CHOICE{Выбор в меню}
        CHOICE -->|Личный кабинет| CAB[Бот: Личный кабинет. Выбери раздел]
        CHOICE -->|Моя ссылка| LINK[Бот: Твоя реферальная ссылка и инструкция]
        CHOICE -->|Статистика| STATS[Бот: Рефералов, место в рейтинге, дата регистрации]
        CHOICE -->|Подсказки| TIPS[Бот: Подсказки по приглашению]
        CHOICE -->|Грейды| GRADES[Бот: Твои грейды, рубежи, до следующего]
        CHOICE -->|Связаться| CONTACT[Бот: Список контактов организаторов]
        CAB --> CAB_BT[Статистика, Ссылка, Подсказки, Грейды, Топ рефереров, Изменить контакты]
        CAB_BT --> CHOICE
        LINK --> CHOICE
        STATS --> CHOICE
        TIPS --> CHOICE
        GRADES --> CHOICE
        CONTACT --> CHOICE
        LINK --> INVITE[Делится ссылкой. Друзья проходят очный этап]
    end

    INVITE --> ADMIN

    subgraph STAGE4[Этап 4. Зачёт]
        ADMIN[Админ загружает CSV из CRM]
        ADMIN --> IMPORT[Бот начисляет рефералов пользователям]
    end

    subgraph STAGE5[Этап 5. Награды]
        IMPORT --> THRESHOLD{Достигнут рубеж?}
        THRESHOLD -->|Да| CONGRATS[Бот: Поздравляем. Ты достиг рубежа. Свяжись с организаторами за наградой]
        THRESHOLD -->|Нет| GRADES
        CONGRATS --> GRADES
    end
```

---

## Этап 1. Вход в бота (без подписки)

Пользователь пишет `/start` и **ещё не подписан** на закрытый канал:

```mermaid
flowchart LR
    subgraph E1[Этап 1. Вход без подписки]
        U1[/start]
        B1[Бот: приветствие и описание программы. Доступ после очного этапа и подписки.]
        K1[Кнопки: Заявка на этап и Проверить подписку]
    end
    U1 --> B1 --> K1
```

Если пользователь **уже подписан** — бот переходит к этапу 2 (регистрация email/телефон или приветствие с кабинетом).

---

## Этап 1 (продолжение). Попытка действия без подписки

Любое действие (например, «Личный кабинет», «Моя ссылка») **без подписки** перехватывается:

```mermaid
flowchart LR
    subgraph Блок["Без подписки"]
        U2[Любая кнопка / команда]
        B2["Бот:\n⚠️ Чтобы пользоваться ботом, нужно пройти очный этап и подписаться на канал.\n\nОставь заявку на очный этап по кнопке ниже. После прохождения — зайди в канал и нажми «Проверить подписку»."]
        K2["Кнопки:\n📋 Оставить заявку на очный этап\n✅ Проверить подписку"]
    end
    U2 --> B2 --> K2
```

При нажатии «Проверить подписку» без подписки:

```mermaid
flowchart LR
    A[Нажатие «Проверить подписку»] --> B["Бот (всплывающее окно):\n❌ Ты ещё не в закрытом канале.\nПодпишись после прохождения очного этапа."]
```

---

## Этап 2. Подписка подтверждена → регистрация (email, телефон)

После очного этапа пользователь подписывается на канал и нажимает «Проверить подписку»:

```mermaid
flowchart TB
    subgraph Этап2["Этап 2. Подписка и регистрация"]
        S[«Проверить подписку»]
        OK["Бот: ✅ Подписка подтверждена!"]
        S --> OK

        NO_EMAIL["Бот:\n🎉 Отлично, {имя}!\n\nОчный этап пройден. Для участия в реферальной программе введи свой email, который указывал при регистрации на сайте.\n\nЭто нужно для связки анкеты с ботом."]
        OK --> NO_EMAIL

        USER_EMAIL[Пользователь вводит email]
        NO_EMAIL --> USER_EMAIL

        BAD_EMAIL["Бот:\n❌ Неверный формат email. Попробуй ещё раз.\nПример: example@mail.ru"]
        USER_EMAIL --> BAD_EMAIL

        SAVE_EMAIL["Бот:\n✅ Email сохранён!\n\nТеперь укажи номер телефона. Нажми кнопку ниже или напиши номер вручную."]
        USER_EMAIL --> SAVE_EMAIL

        K_PHONE["Кнопка: 📱 Поделиться контактом"]
        SAVE_EMAIL --> K_PHONE

        BAD_PHONE["Бот:\n❌ Введи корректный номер (например: +79001234567)."]
        K_PHONE --> BAD_PHONE

        DONE["Бот сохраняет телефон → показывает приветствие и меню"]
        K_PHONE --> DONE
    end
```

---

## Этап 2 (продолжение). После сохранения телефона — три сообщения

После ввода телефона бот отправляет **три сообщения подряд**:

```mermaid
flowchart TB
    subgraph Три_сообщения["Сообщения после сохранения телефона"]
        M1["1️⃣ Бот:\n✅ Номер сохранён!\n\nТеперь можно участвовать в реферальной программе.\n\n📌 Твоя задача:\n1. Получаешь свою реферальную ссылку\n2. Приглашаешь друзей на очный этап по своей ссылке\n3. За каждого прошедшего тебе начисляются баллы\n4. Набираешь больше баллов — повышаешь грейд!\n\n🎁 Каждый новый грейд = мерч от «Алабуги». За 5 человек — 1 грейд и рюкзак 🎒"]
        M2["2️⃣ Бот: [фото рюкзака backpack.png]"]
        M3["3️⃣ Бот:\nЭто не розыгрыш — ты точно знаешь, сколько нужно пригласить.\n\nГотов забрать свой рюкзак и пойти дальше?"]
        K3["Кнопки меню: Личный кабинет, Моя ссылка, Статистика, Подсказки, Грейды (+ Связаться при включённой настройке)"]
        K4["Кнопки кабинета: 📊 Моя статистика, 🔗 Моя ссылка, 💡 Подсказки, 📊 Грейды, 🏆 Топ рефереров, ✏️ Изменить контакты"]
    end
    M1 --> M2 --> M3
    M1 --> K3
    M3 --> K4
```

---

## Этап 2 → 3. Личный кабинет и «Моя ссылка»

После регистрации пользователь видит основное меню и может открыть кабинет или ссылку:

```mermaid
flowchart TB
    subgraph Кабинет["Личный кабинет"]
        L1["Пользователь: 👤 Личный кабинет"]
        B_L1["Бот:\n📋 Личный кабинет\n\nВыбери нужный раздел:"]
        L1 --> B_L1
        B_L1 --> Bt["Кнопки: 📊 Моя статистика | 🔗 Моя ссылка | 💡 Подсказки | 📊 Грейды | 🏆 Топ рефереров | ✏️ Изменить контакты"]
    end

    subgraph Ссылка["Моя ссылка"]
        L2["Пользователь: 🔗 Моя ссылка"]
        B_L2["Бот:\n🔗 Твоя реферальная ссылка\n\n<code>https://polytech.alabuga.ru/?utm_...</code>\n\n👆 Нажми, чтобы скопировать.\n\n📢 Как это работает:\n1. Отправь ссылку другу\n2. Друг регистрируется на очный этап через неё\n3. Когда друг пройдёт очный этап — тебе засчитается реферал\n\n📊 Достигай рубежей — смотри раздел «Грейды»."]
        L2 --> B_L2
    end
```

---

## Примеры: Статистика и Грейды

```mermaid
flowchart TB
    subgraph Статистика["📊 Статистика"]
        St["Пользователь: 📊 Статистика"]
        B_St["Бот:\n📊 Твоя статистика\n\n👤 ID: ...\n📅 Дата регистрации: ...\n\n👥 Рефералов: N\n🏆 Место в рейтинге: #N"]
        St --> B_St
    end

    subgraph Грейды["📊 Грейды"]
        Gr["Пользователь: 📊 Грейды"]
        B_Gr["Бот:\n📊 Твои грейды\n\n👥 Рефералов: N\n\n• 5 реф → рюкзак — ✅ Достигнут (ожидает выдачи)\n• 10 реф → мерч — 🔒 Ещё 3 реф.\n\n🎯 До следующего рубежа: 3 рефералов."]
        Gr --> B_Gr
    end
```

---

## Этап 4–5. Зачёт рефералов и уведомление о грейде

Организаторы загружают CSV — бот начисляет рефералов. При достижении рубежа пользователю приходит:

```mermaid
flowchart LR
    subgraph Уведомление["Сообщение при достижении грейда"]
        N["Бот (после импорта CSV):\n🎉 Поздравляем!\n\nТы достиг рубежа N рефералов.\n\nТвои награды: рюкзак, мерч\n\nСвяжись с организаторами для получения."]
    end
```

---

## Общая схема этапов (кратко)

```mermaid
flowchart TB
    subgraph E1["Этап 1. Вход"]
        E1A["/start → Бот: приветствие + обязательная подписка на канал\nКнопки: Оставить заявку | Проверить подписку"]
    end
    subgraph E2["Этап 2. Подписка и регистрация"]
        E2A["Проверить подписку → Бот: запрос email → запрос телефона\n→ 3 сообщения: приветствие, фото рюкзака, кнопка кабинета"]
    end
    subgraph E3["Этап 3. Приглашение"]
        E3A["Моя ссылка → делить ссылкой, друзья проходят очный этап"]
    end
    subgraph E4["Этап 4. Зачёт"]
        E4A["Админ загружает CSV → бот начисляет рефералов"]
    end
    subgraph E5["Этап 5. Грейды"]
        E5A["Раздел Грейды: прогресс, рубежи. При достижении — поздравление от бота"]
    end
    E1 --> E2 --> E3 --> E4 --> E5
```

---

*Диаграммы построены по реальным текстам из бота (`start.py`, `cabinet.py`, `grade.py`, `middlewares/subscription.py`). Открыть в редакторе с поддержкой Mermaid или на GitHub.*
